{
  "name": "webtor-rs-workspace",
  "private": true,
  "version": "0.5.15",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "build": "wasm-pack build webtor-wasm --release --target web --scope andrewtheguy",
    "test": "bun test --timeout 60000 ./tests/api.test.mjs",
    "test:live": "bun test --timeout 600000 ./tests/live.test.mjs",
    "seed": "bun run ./tests/tools/fetch-directory.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.50.0",
    "wasm-pack": "0.15.0"
  },
  "trustedDependencies": [
    "wasm-pack"
  ]
}
